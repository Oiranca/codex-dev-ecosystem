# Handoff Guidelines

Load when the user asks to "update handoff" or equivalent.

- default target is a repo-local handoff document such as `docs/HANDOFF.md`
- do not post handoff state to GitHub PR comments, review replies, issues, or other external surfaces unless the user explicitly asks
- if the repo has no handoff document, create or update a local doc first, then mention the gap to the user
